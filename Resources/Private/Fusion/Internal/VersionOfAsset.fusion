prototype(Carbon.IncludeAssets:VersionOfAsset) < prototype(Neos.Fusion:Value) {
    path = ${path}

    @context.path = ${this.path}

    hash = Carbon.FileContent:HashOfFile {
        path = ${path}
    }

    value = ${String.isBlank(this.hash) ? '' : '?h=' + this.hash}
}
