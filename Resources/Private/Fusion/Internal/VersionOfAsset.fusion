prototype(Carbon.IncludeAssets:VersionOfAsset) < prototype(Neos.Fusion:Value) {
    path = ${path}

    hash = ${Carbon.FileContent.pathHash(this.path)}

    isFirst = true
    divider = ${this.isFirst ? '?' : '&'}

    value = ${String.isBlank(this.hash) ? '' : this.divider + 'h=' + this.hash}
}
