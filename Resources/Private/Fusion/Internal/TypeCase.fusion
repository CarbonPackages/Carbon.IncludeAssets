prototype(Carbon.IncludeAssets:TypeCase) < prototype(Neos.Fusion:Case) {
  isCSS {
    condition = ${String.toUpperCase(type) == 'CSS' ? true : false}
    renderer = ''
  }
  isJS {
    condition = ${String.toUpperCase(type)  == 'JS' ? true : false}
    renderer = ''
  }
}
