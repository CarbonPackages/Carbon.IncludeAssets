{
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "prettier": "prettier --write --no-error-on-unmatched-pattern '**/*.{js,yaml,md,php}'",
    "pipeline": "pnpm install;pnpm prettier"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.24.0",
    "prettier": "^3.6.2"
  }
}
